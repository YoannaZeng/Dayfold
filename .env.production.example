# Copy these values into the hosting provider or an ignored local shell file.
# Do not commit real production secrets.
DATABASE_URL="postgresql://user:password@host:5432/dayfold?schema=public"
DAYFOLD_SESSION_SECRET="replace-with-a-stable-random-secret-at-least-32-characters"
DAYFOLD_PUBLIC_ORIGIN="https://your-production-domain.com"
